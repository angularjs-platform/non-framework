const moduleName: string = 'non.framework.components.forms.formly.widgets.gridmultipleitems';

import {FormlyConfig} from './gridmultipleitems.config';

angular.module(moduleName, [])
    .config(FormlyConfig);

export default moduleName;
