const moduleName: string = 'non.framework.components.forms.formly.widgets.checkbox';

import {FormlyConfig} from './checkbox.config';

angular.module(moduleName, [])
    .config(FormlyConfig);

export default moduleName;
