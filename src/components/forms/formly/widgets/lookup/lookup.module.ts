const moduleName: string = 'non.framework.components.forms.formly.widgets.lookup';

import {FormlyConfig} from './lookup.config';

angular.module(moduleName, [])
    .config(FormlyConfig);

export default moduleName;
